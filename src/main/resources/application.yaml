server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/trello_app
    username: root
    password: INDIANa@1992
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

logging:
  level:
    org.hibernate.SQL: DEBUG